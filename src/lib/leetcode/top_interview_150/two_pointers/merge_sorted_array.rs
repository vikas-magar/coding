/* https://leetcode.com/problems/merge-sorted-array/description/ 88 */
use crate::Solution;

impl Solution {
    pub fn merge(m: i32, n: i32) {
        let m = m as usize;
        let n = n as usize;
        while m < n {}
    }
}

#[cfg(test)]
mod tests {
    #[test]
    fn test_merge_list() {}
}
